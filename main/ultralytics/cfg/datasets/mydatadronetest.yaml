# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
#path: datasets/LLVIP700  # dataset root dir

# 下面这些路径是固定的，都不要改，只改上面的'path'
train: /root/autodl-tmp/datasets/drone/mydata/images/train  # train images (relative to 'path') 128 images
val: /root/autodl-tmp/datasets/drone/mydata/images/test  # val images (relative to 'path') 128 images
train_labels: /root/autodl-tmp/datasets/drone/mydata/labels/train
val_labels: /root/autodl-tmp/datasets/drone/mydata/labels/test


# Classes
names:
    0: bus
    1: car
    2: van
    3: feright_car
    4: truck
